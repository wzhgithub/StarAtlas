volumes:
  data:

services:
  mongodb:
    image: mongo:latest
    environment:
      - MONGO_INITDB_ROOT_USERNAME=docker
      - MONGO_INITDB_ROOT_PASSWORD=mongopw
    ports:
      - 27017:27017
    volumes:
      - data:/data/db
  myapp:
    image: star_atlas_server:latest 
    network_mode: "host"
    container_name: "star_atlas_server"
    ports:
      - 9999:9999
